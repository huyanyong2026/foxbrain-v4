# FoxBrain Operations

Daily checks:

```bash
bash healthcheck.sh
docker compose ps
```

Cloud services use `restart: always`, so the system keeps running after the personal computer is turned off and after the cloud server restarts.

