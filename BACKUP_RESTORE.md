# Backup And Restore

Run backup:

```bash
cd /opt/foxbrain
bash backup.sh
```

Restore:

```bash
cd /opt/foxbrain
bash restore.sh /opt/foxbrain/backups/BACKUP_DIR
```

Backups should not be committed to GitHub.

